import { DynamicEventsDirective } from './dynamic-events.directive';

describe('DynamicEvents', () => {
  it('should create an instance', () => {
    const directive = new DynamicEventsDirective();
    expect(directive).toBeTruthy();
  });
});
