import { UiDropdownHoverDirective } from './ui-dropdown-hover.directive';

describe('UiDropdownHover', () => {
  it('should create an instance', () => {
    const directive = new UiDropdownHoverDirective();
    expect(directive).toBeTruthy();
  });
});
