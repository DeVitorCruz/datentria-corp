import { StickyOnScroll } from './sticky-on-scroll';

describe('StickyOnScroll', () => {
  it('should create an instance', () => {
    const directive = new StickyOnScroll();
    expect(directive).toBeTruthy();
  });
});
